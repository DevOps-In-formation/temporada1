name: En PR

on:
  pull_request:
    branches:
      - episodio*

jobs:
  job1:
    name: Hacer checkout
    runs-on: ubuntu-latest
    steps:
      - name: check out
        uses: actions/checkout@v5
        with:
          ref: ${{ github.ref || github.sha }}
